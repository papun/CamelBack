package com.dgf.demo_dgf.view;


/*import com.dgf.demo_dgf.dto.DgfServiceDto;
import com.vaadin.flow.component.button.Button;
import com.vaadin.flow.component.grid.Grid;
import com.vaadin.flow.component.orderedlayout.VerticalLayout;
import com.vaadin.flow.router.Route;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.client.RestTemplate;

import java.util.Arrays;
import java.util.List;

@Route("/")*/
public class DgfServiceView/* extends VerticalLayout */{

	private static final long serialVersionUID = 981962251061306974L;

	/*
	 * private final Grid<DgfServiceDto> grid; private final RestTemplate
	 * restTemplate;
	 * 
	 * @Autowired public DgfServiceView() { this.restTemplate = new RestTemplate();
	 * this.grid = new Grid<>(DgfServiceDto.class);
	 * 
	 * add(new Button("Refresh", event -> fetchDataFromApi())); add(grid);
	 * fetchDataFromApi(); }
	 * 
	 * private void fetchDataFromApi() { String url =
	 * "http://localhost:8080/dgf/configuration"; DgfServiceDto[] dtos =
	 * restTemplate.getForObject(url, DgfServiceDto[].class); if (dtos != null) {
	 * List<DgfServiceDto> dtoList = Arrays.asList(dtos); grid.setItems(dtoList);
	 * grid.setColumns("id", "name", "oderable", "billable", "billing_name"); } }
	 */
    
}